@{ 
    var carousels = ViewBag.Carousels as IEnumerable<Carousel>;
}
<!--
            作者：susd
            公众号：苏克主任
            时间：2020-10-29
            描述：轮播
        -->
<div id="carouselExampleFade" class="carousel slide carousel-fade" data-ride="carousel">
    <div class="carousel-inner">
        @foreach (var c in carousels)
        {
            if (carousels.ToList().IndexOf(c) == 0)
            {
                <div class="carousel-item active">
                    <img data-original="@(c.Url)" class="d-block w-100" alt="@(c.Name)">
                </div>
            }
            else
            {
                <div class="carousel-item">
                    <img src="@(c.Url)" class="d-block w-100" alt="@(c.Name)">
                </div>
            }
        }

    </div>
    @if (carousels!=null && carousels.Count() > 1)
    {
        <a class="carousel-control-prev" href="#carouselExampleFade" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">上一张</span>
        </a>
        <a class="carousel-control-next" href="#carouselExampleFade" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">下一张</span>
        </a>
    }
</div>